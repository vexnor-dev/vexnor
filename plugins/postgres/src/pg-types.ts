export type Point = { x: number; y: number };
export type Circle = { x: number; y: number; radius: number };
export type Interval = { years?: number; months?: number; days?: number; hours?: number; minutes?: number; seconds?: number; milliseconds?: number };
