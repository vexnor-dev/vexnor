# vexnor-sequelize

Sequelize adaptor for Vexnor.

Convert Sequelize models (tables or views) into Vexnor runtime tables.

```ts
import { fromSequelizeTable, fromSequelizeView } from "vexnor-sequelize";
```
