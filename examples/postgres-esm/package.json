{
   "name": "@valnor/example-postgres-esm",
   "version": "1.0.0",
   "description": "",
   "type": "module",
   "module": "./dist/index.js",
   "scripts": {
      "precodegen": "pnpm run --filter='valnor' --filter='valnor-postgres' build",
      "codegen": "env-cmd --no-override -x -r ../../env-dev.json -e postgres node ../../packages/valnor/cli.mjs codegen --plugin valnor-postgres --schema valnor_test --pascalCaseTables --camelCaseColumns --host \\$POSTGRES_HOST --port \\$POSTGRES_PORT --database \\$POSTGRES_DATABASE --user \\$POSTGRES_USER --password \\$POSTGRES_PASSWORD --outDir './src/codegen' | pino-pretty",
      "prebuild": "run-s 'codegen'",
      "build": "tsc -b",
      "predev": "run-s 'build'",
      "dev": "run-s 'dev:*'",
      "dev:1-start": "env-cmd --no-override -x -r ../../env-dev.json -e postgres vite-node src/index.ts | pino-pretty"
   },
   "keywords": [],
   "author": "",
   "license": "ISC",
   "dependencies": {
      "pg": "^8.16.3",
      "valnor": "workspace:^1.0.0",
      "valnor-postgres": "workspace:^1.0.0"
   },
   "devDependencies": {
      "@types/pg": "^8.15.5",
      "vite-tsconfig-paths": "^5.1.4",
      "vitest": "^3.2.4"
   }
}
